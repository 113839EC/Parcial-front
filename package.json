{
  "name": "dominio",
  "version": "1.0.0",
  "description": "Dominio — juego de estrategia por turnos",
  "type": "module",
  "scripts": {
    "dev": "concurrently \"npm run api\" \"vite\"",
    "build": "vite build",
    "preview": "vite preview",
    "api": "json-server --watch public/data/ranking.json --port 3001"
  },
  "dependencies": {
    "json-server": "^0.17.4"
  },
  "devDependencies": {
    "autoprefixer": "^10.4.21",
    "concurrently": "^9.2.1",
    "postcss": "^8.5.3",
    "tailwindcss": "^3.4.17",
    "vite": "^6.3.5"
  }
}
