{
  "name": "parcial-front",
  "version": "1.0.0",
  "description": "Juego de cartas - Frontend",
  "scripts": {
    "api": "json-server --watch db.json --port 3000",
    "build": "tsc",
    "watch": "tsc --watch",
    "dev": "concurrently \"npm run api\" \"npm run watch\""
  },
  "dependencies": {
    "json-server": "^0.17.4"
  },
  "private": true,
  "devDependencies": {
    "concurrently": "^9.2.1",
    "typescript": "^6.0.3"
  }
}
